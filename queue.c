#include "monty.h"

/**
 * queue - Changes stack to queue
 * @stack: Pointer to pointer to the top element in stack/queue
 * @line_number: Line number in the monty bytecode file
 *
 * Return: void
 */
void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	s_tack = 0;
}
