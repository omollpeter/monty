#include "monty.h"

/**
 * stack_st - Changes queue to stack
 * @stack: Pointer to pointer to the top element in stack/queue
 * @line_number: Line number in the monty bytecode file
 *
 * Return: void
 */
void stack_st(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	s_tack = 1;
}
