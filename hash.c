#include "monty.h"

/**
 * hash - Ignores instructions starting with #
 * @stack: Pointer to pointer to the the top stack element
 * @line_number:L Line number in the monty bytecode file
 *
 * Return: void
 */
void hash(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;

	;
}
